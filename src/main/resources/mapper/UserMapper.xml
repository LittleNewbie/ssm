<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.svili.crud.UserDao">

	<!-- <resultMap id="BaseResultMap" type="User">
		<id column="user_id" property="userId" jdbcType="INTEGER" />
		<result column="dept_id" property="deptId" jdbcType="INTEGER" />
		<result column="user_name" property="userName" jdbcType="VARCHAR" />
		<result column="login_name" property="loginName" jdbcType="VARCHAR" />
		<result column="password_alias" property="password" jdbcType="VARCHAR" />
		<result column="mobile_phone" property="mobilePhone" jdbcType="VARCHAR" />
		<result column="office_phone" property="officePhone" jdbcType="VARCHAR" />
		<result column="email" property="email" jdbcType="VARCHAR" />
		<result column="job" property="job" jdbcType="VARCHAR" />
		<result column="order_id" property="orderId" jdbcType="INTEGER" />
		<result column="state" property="state"
			typeHandler="org.apache.ibatis.type.EnumOrdinalTypeHandler" />
	</resultMap> -->
<!--resultType="User"  -->
	<!-- <select id="select" parameterType="object" resultMap="BaseResultMap"> -->
	<select id="select" parameterType="object" resultType="User">
		select *
		from ybg_test_user
		where user_id = #{primaryValue}
	</select>
	
	<insert id="insert" parameterType="User">
		insert into ybg_test_user (user_id,dept_id,
		user_name,
		login_name, password_alias, mobile_phone,
		office_phone, email, job,
		order_id, state)
		values (#{userId,jdbcType=INTEGER},#{deptId,jdbcType=INTEGER},
		#{userName,jdbcType=VARCHAR},
		#{loginName,jdbcType=VARCHAR},
		#{password,jdbcType=VARCHAR}, #{mobilePhone,jdbcType=VARCHAR},
		#{officePhone,jdbcType=VARCHAR}, #{email,jdbcType=VARCHAR},
		#{job,jdbcType=VARCHAR},
		#{orderId,jdbcType=INTEGER},
		#{state,typeHandler=org.apache.ibatis.type.EnumOrdinalTypeHandler})
	</insert>
</mapper>